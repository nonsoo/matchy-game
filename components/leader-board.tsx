import styles from "@styles/components/leader-board.module.css";
import Player_badge from "./player-badge";

const Leader_board = () => {
  return <section className={styles.mainWrapper}>Leader_board</section>;
};

export default Leader_board;
