import styles from "./styles/generic-comp.module.css";

const first_word_loader = () => {
  return <p className={styles.first_word_loader}>Your first word is...</p>;
};

export default first_word_loader;
