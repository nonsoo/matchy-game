import { Sansita, Overpass } from "next/font/google";

export const sansita = Sansita({
  weight: ["400", "700", "800"],
  subsets: ["latin"],
});

export const overpass = Overpass({ subsets: ["latin"] });
